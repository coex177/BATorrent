# Build
build/
build-release/

# macOS
.DS_Store

# clangd (symlink to build/compile_commands.json)
compile_commands.json

# Local secrets (TMDB / IGDB API keys) — never commit
.env

# IDE
.vscode/
.idea/
.cache/
*.swp
*~

# Claude Code — everything local only, never in the public repo (settings,
# skills, and the operational PR-review workflows).
.claude/
/CLAUDE.md
.github/workflows/claude.yml
.github/workflows/claude-code-review.yml

# Compiled
*.o
*.so
*.a

# Internal docs / notes (not for the public repo) — drop anything in here
/internal/

# local test build dir
build-tests/
build-asan/

# fork-from-source build dir
build-fork/
