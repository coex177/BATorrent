# Build
build/
build-release/

# Release packaging artifacts (regenerated by the deploy/installer build)
release/
output/
installer/wizard_large.bmp
installer/wizard_small.bmp

# macOS
.DS_Store

# clangd (symlink to build/compile_commands.json)
compile_commands.json

# Local secrets (TMDB / IGDB API keys) — never commit
.env

# IDE
.vscode/
.idea/
.cache/
*.swp
*~

# Claude Code local settings
.claude/settings.local.json

# Compiled
*.o
*.so
*.a

# Internal docs / notes (not for the public repo) — drop anything in here
/internal/

# local test build dir
build-tests/
build-asan/
