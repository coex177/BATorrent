# Build
build/
build-release/

# clangd (symlink to build/compile_commands.json)
compile_commands.json

# Local secrets (TMDB / IGDB API keys) — never commit
.env

# IDE
.vscode/
.idea/
.cache/
*.swp
*~

# Claude Code local settings
.claude/settings.local.json

# Compiled
*.o
*.so
*.a

# Internal docs (not for public repo)
MARKETING_PLAN.md

# local test build dir
build-tests/
build-asan/
